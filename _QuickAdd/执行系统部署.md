```js
module.exports = {
    entry: async (params, settings) => {
        const app = params.app;

        // ============================================================
        // 1. 硬编码的部署数据
        // ============================================================
        const data = {
            "system_name": "Obsidian自动知识库部署架构",
            "description": "基于Codex AI助手的全自动知识捕获、处理、归档与输出系统，集成多源信息输入、自动化处理插件与结构化存储流程",
            "folders": [
                { "path": "00_Inbox", "purpose": "原始抓取内容临时存放区" },
                { "path": "01_Sources", "purpose": "来源资料归档区" },
                { "path": "02_Projects", "purpose": "项目工作区（预留）" },
                { "path": "03_Knowledge", "purpose": "知识库核心区（预留）" },
                { "path": "04_Outputs", "purpose": "输出成果区" },
                { "path": "05_Review", "purpose": "每日简报与复盘区" },
                { "path": "99_Attachments", "purpose": "附件资源区" }
            ],
            "files": [
                {
                    "path": "00_Inbox/README.md",
                    "purpose": "说明收件箱用途与处理流程",
                    "content": "# 00_Inbox - 收件箱\n\n此文件夹用于存放自动抓取或手动剪藏的内容，等待进一步处理。\n\n- GitHub Trending / RSS 抓取 → 自动落盘\n- Web Clipper 剪辑 → 手动暂存\n- 社交媒体导入 → 手动或半自动归档\n\n处理完成后应移入对应 source 或 knowledge 目录。"
                },
                {
                    "path": "05_Review/README.md",
                    "purpose": "说明每日简报生成规则与历史归档方式",
                    "content": "# 05_Review - 复习与简报\n\n由 Codex 每日自动生成简报，内容包括：\n- 当日新增知识摘要\n- 关键链接与来源\n- 待处理事项提醒\n\n支持按日期归档，便于回顾与检索。"
                },
                {
                    "path": "99_Attachments/README.md",
                    "purpose": "说明附件管理规范",
                    "content": "# 99_Attachments - 附件资源\n\n存放所有非文本资源：\n- 图片（含AI生成配图）\n- 视频缩略图\n- PDF / DOCX 原始文件（如需保留）\n\n建议命名规范：YYYYMMDD_描述.ext"
                }
            ],
            "plugins": [
                { "name": "GitHub定时抓取 Horizon", "purpose": "定时从 GitHub Trending、自定义仓库抓取项目动态", "target": "00_Inbox", "setup": "需配置 GitHub API Key、仓库地址、抓取关键词、时间间隔" },
                { "name": "Claudian 插件", "purpose": "使 Codex AI 可直接读写整个 Vault", "target": "全库", "setup": "需填写 Codex CLI 路径，连接桌面端 Codex 与 Obsidian 仓库权限" },
                { "name": "Web Clipper / Clipper 插件", "purpose": "剪藏网页文章、视频字幕、灵感片段", "target": "00_Inbox", "setup": "安装浏览器扩展，配置目标文件夹为 00_Inbox" },
                { "name": "社媒导入插件", "purpose": "从小红书、微信公众号、微博等平台导入内容", "target": "00_Inbox 或 01_Sources/社媒内容", "setup": "需授权账号权限，设置抓取频率" },
                { "name": "Feishu CLI", "purpose": "将飞书文档、会议纪要、团队资料自动导入", "target": "01_Sources/会议纪要 或 01_Sources/旧资料导入", "setup": "配置飞书应用凭证，设置文档筛选规则" },
                { "name": "Docxer 插件", "purpose": "将 Word / 飞书导出文档转换为 Markdown", "target": "01_Sources/旧资料导入", "setup": "安装后拖拽文档即可转换" },
                { "name": "Importer 插件", "purpose": "迁移 Notion、Apple Notes 等旧笔记库", "target": "01_Sources/旧资料导入", "setup": "选择源平台，配置导出格式" },
                { "name": "Markdown排版 / 公众号排版插件", "purpose": "对输出文章进行标准化排版", "target": "04_Outputs/文章", "setup": "预设模板，支持一键格式化" },
                { "name": "HyperFrames 插件", "purpose": "从视频中提取关键帧、生成摘要与介绍文本", "target": "04_Outputs/视频脚本 或 04_Outputs/视频摘要", "setup": "上传视频文件或指定路径，配置 AI 分析模型" },
                { "name": "配图 Skill + Image 模型", "purpose": "根据文章内容自动生成配图", "target": "99_Attachments/图片 及 04_Outputs/文章", "setup": "接入 AI 图像生成服务，配置提示词模板" }
            ],
            "services": [
                { "name": "Codex AI 助手", "purpose": "作为大脑中枢，驱动所有自动化流程", "target": "全库协调", "setup": "需配置 API Key、CLI 路径、Obsidian Vault 路径" },
                { "name": "GitHub 定时抓取服务", "purpose": "每日自动拉取技术趋势、热门项目", "target": "00_Inbox", "setup": "通过 Horizon 插件设置 Cron 任务" },
                { "name": "飞书自动化导入服务", "purpose": "实时同步会议记录、项目文档", "target": "01_Sources/会议纪要", "setup": "需开通飞书开放平台应用" },
                { "name": "社交媒体监控服务", "purpose": "追踪指定账号、话题的最新动态并剪藏", "target": "00_Inbox 或 01_Sources/社媒内容", "setup": "需各平台授权，设置关键词与去重逻辑" },
                { "name": "AI 配图生成服务", "purpose": "为文章自动匹配视觉素材", "target": "99_Attachments + 04_Outputs", "setup": "接入图像生成 API，设定风格偏好" }
            ],
            "manual_setup": [
                { "name": "Codex API Key", "reason": "用于调用 AI 能力进行内容理解、摘要、配图生成等操作", "required": true },
                { "name": "GitHub Token / API Key", "reason": "用于访问 GitHub Trending 和私有仓库数据", "required": false },
                { "name": "飞书应用权限", "reason": "用于读取企业内部文档与会议记录", "required": false },
                { "name": "社交媒体账号授权", "reason": "用于自动抓取小红书、公众号等内容", "required": false },
                { "name": "Codex CLI 路径配置", "reason": "确保本地客户端能正确调用 Codex", "required": true },
                { "name": "Obsidian Vault 路径绑定", "reason": "让 Codex 能够识别并写入正确的知识库目录", "required": true },
                { "name": "抓取关键词与频率设定", "reason": "控制信息摄入的质量与节奏", "required": true }
            ],
            "automation": [
                { "name": "GitHub Trending 每日抓取", "trigger": "定时任务（每日 8:00）", "input": "GitHub Trending / 指定仓库", "output": "00_Inbox" },
                { "name": "网页剪藏自动归档", "trigger": "用户触发 Web Clipper", "input": "浏览器剪藏动作", "output": "00_Inbox" },
                { "name": "飞书会议纪要自动同步", "trigger": "新文档创建或更新事件", "input": "飞书文档系统", "output": "01_Sources/会议纪要" },
                { "name": "Word/DOCX 文档转 Markdown", "trigger": "文件放入指定输入目录", "input": "01_Sources/待转换文档", "output": "01_Sources/旧资料导入" },
                { "name": "视频内容解析与脚本生成", "trigger": "上传视频或指定视频链接", "input": "视频文件或 URL", "output": "04_Outputs/视频脚本 + 04_Outputs/视频摘要" },
                { "name": "AI 配图自动生成与插入", "trigger": "文章保存或发布前钩子", "input": "04_Outputs/文章", "output": "99_Attachments/图片 + 文章内嵌入配图链接" },
                { "name": "每日简报自动生成", "trigger": "定时任务（每日 20:00）", "input": "00_Inbox + 01_Sources 新增内容", "output": "05_Review/每日简报.md" },
                { "name": "社媒内容监控与剪藏", "trigger": "定时轮询或推送通知", "input": "小红书 / 公众号 / 微博等", "output": "00_Inbox 或 01_Sources/社媒内容" }
            ]
        };

        // ============================================================
        // ★ 总文件夹名称：使用系统名称
        // ============================================================
        const BASE_FOLDER = data.system_name || "Obsidian自动知识库部署架构";

        // ============================================================
        // 辅助函数：递归创建文件夹（自动加总文件夹前缀）
        // ============================================================
        async function ensureFolder(folderPath) {
            if (!folderPath || folderPath === "/" || folderPath === "") return;
            const parts = folderPath.split("/").filter(p => p !== "");
            let current = BASE_FOLDER; // 从总文件夹开始
            for (const part of parts) {
                current = `${current}/${part}`;
                if (!app.vault.getAbstractFileByPath(current)) {
                    try {
                        await app.vault.createFolder(current);
                        console.log(`✅ 创建文件夹：${current}`);
                    } catch (e) {
                        console.error(`❌ 创建文件夹失败：${current}`, e);
                        new Notice(`创建文件夹失败：${current}`);
                    }
                }
            }
        }

        // 确保总文件夹本身存在
        await ensureFolder("");

        // ============================================================
        // 创建所有顶层文件夹（在总文件夹下）
        // ============================================================
        for (const folder of data.folders) {
            await ensureFolder(folder.path);
        }

        // ============================================================
        // 创建额外的子文件夹（在总文件夹下）
        // ============================================================
        const subFolders = [
            "00_Inbox/自动抓取",
            "01_Sources/会议纪要",
            "01_Sources/旧资料导入",
            "01_Sources/社媒内容",
            "04_Outputs/文章",
            "04_Outputs/视频脚本",
            "04_Outputs/视频摘要",
            "99_Attachments/图片",
            "99_Attachments/视频"
        ];
        for (const sub of subFolders) {
            await ensureFolder(sub);
        }

        // ============================================================
        // 创建 README 文件（在总文件夹下）
        // ============================================================
        for (const fileItem of data.files) {
            const relativePath = fileItem.path;
            if (!relativePath.endsWith(".md")) continue;
            const fullPath = `${BASE_FOLDER}/${relativePath}`;
            if (app.vault.getAbstractFileByPath(fullPath)) {
                console.warn(`⚠️ 文件已存在，跳过：${fullPath}`);
                continue;
            }
            const parent = fullPath.split("/").slice(0, -1).join("/");
            await ensureFolder(parent.replace(BASE_FOLDER + "/", "")); // 确保父文件夹存在
            try {
                await app.vault.create(fullPath, fileItem.content || "");
                console.log(`✅ 创建文件：${fullPath}`);
            } catch (e) {
                console.error(`❌ 创建文件失败：${fullPath}`, e);
                new Notice(`创建文件失败：${fullPath}`);
            }
        }

        // ============================================================
        // 创建“系统配置”文件夹（在总文件夹下）
        // ============================================================
        const configFolder = "系统配置";
        await ensureFolder(configFolder);

        // 辅助函数：创建或更新文件（自动加总文件夹前缀）
        async function createOrUpdateFile(relativeFolder, fileName, content) {
            const folderFull = `${BASE_FOLDER}/${relativeFolder}`;
            await ensureFolder(relativeFolder);
            const filePath = `${folderFull}/${fileName}`;
            const existing = app.vault.getAbstractFileByPath(filePath);
            if (existing) {
                await app.vault.modify(existing, content);
                console.log(`🔄 更新文件：${filePath}`);
            } else {
                await app.vault.create(filePath, content);
                console.log(`✅ 创建文件：${filePath}`);
            }
        }

        // 6.1 系统说明.md
        const systemDescriptionContent = `# 系统说明\n\n**系统名称**：${data.system_name}\n\n**系统用途**：${data.description}\n\n**创建时间**：${new Date().toLocaleString()}\n\n---\n\n本文件由自动部署脚本生成。\n`;
        await createOrUpdateFile(configFolder, "系统说明.md", systemDescriptionContent);

        // 6.2 插件安装清单.md
        let pluginLines = [];
        pluginLines.push("# 插件安装清单\n");
        pluginLines.push("> 以下插件需要你手动在 Obsidian 社区插件市场安装并启用。\n");
        for (const plugin of data.plugins) {
            pluginLines.push(`## ${plugin.name}`);
            pluginLines.push(`- **作用**：${plugin.purpose || '无'}`);
            pluginLines.push(`- **数据保存位置**：${plugin.target || '未指定'}`);
            pluginLines.push(`- **配置说明**：${plugin.setup || '无'}`);
            pluginLines.push("");
        }
        await createOrUpdateFile(configFolder, "插件安装清单.md", pluginLines.join("\n"));

        // 6.3 外部服务清单.md
        let serviceLines = [];
        serviceLines.push("# 外部服务清单\n");
        serviceLines.push("> 以下外部服务需要你手动注册、授权并配置。\n");
        for (const service of data.services) {
            serviceLines.push(`## ${service.name}`);
            serviceLines.push(`- **用途**：${service.purpose || '无'}`);
            serviceLines.push(`- **输出位置**：${service.target || '未指定'}`);
            serviceLines.push(`- **配置说明**：${service.setup || '无'}`);
            serviceLines.push("");
        }
        await createOrUpdateFile(configFolder, "外部服务清单.md", serviceLines.join("\n"));

        // 6.4 人工配置清单.md
        let manualLines = [];
        manualLines.push("# 人工配置清单\n");
        manualLines.push("> 以下项目必须由你手动填写或授权，脚本无法自动完成。\n");
        for (const item of data.manual_setup) {
            const required = item.required ? "🔴 必须" : "🟡 可选";
            manualLines.push(`## ${item.name}  ${required}`);
            manualLines.push(`- **原因**：${item.reason || '无'}`);
            manualLines.push("");
        }
        await createOrUpdateFile(configFolder, "人工配置清单.md", manualLines.join("\n"));

        // 6.5 自动化任务清单.md
        let automationLines = [];
        automationLines.push("# 自动化任务清单\n");
        automationLines.push("> 以下自动化任务需要你后续在 QuickAdd 或相关插件中配置触发方式。\n");
        for (const auto of data.automation) {
            automationLines.push(`## ${auto.name}`);
            automationLines.push(`- **触发方式**：${auto.trigger || '未指定'}`);
            automationLines.push(`- **输入来源**：${auto.input || '未指定'}`);
            automationLines.push(`- **输出位置**：${auto.output || '未指定'}`);
            automationLines.push("");
        }
        await createOrUpdateFile(configFolder, "自动化任务清单.md", automationLines.join("\n"));

        new Notice(`✅ 系统部署完成！所有内容都在“${BASE_FOLDER}”文件夹下。`);
    },
    settings: {
        name: "执行系统部署（安全版）",
        author: "QuickAdd",
        options: {}
    }
};