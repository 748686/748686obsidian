# 系统配置说明

### 第一次必须设置
1. `rss_sources.json`：把你之前整理好的全球 RSS 地址放进去。
2. `Skills/`：复制你已有的 27 个 Skill 文件。
3. GitHub Actions Secrets：设置 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。
4. 如果要飞书，再设置 `FEISHU_WEBHOOK`。

### 不要把 API Key 写进任何 `.json`、`.md` 或 `.yml` 文件。
